(function(angular){
	'use strict';

angular.module('fimepedApp')
    .factory('CurrentRevision', function ($resource) {
        return $resource('api/currentRevisions/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    }).factory('CurrentRevisionService', function ($resource) {
        return $resource('api/episodes/:episode_id/currentRevisions/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
}(window.angular));